<form method="post" action="{{ path('app_manif_delete', {'id': manif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manif.id) }}">
    <button class="btn">Delete</button>
</form>
